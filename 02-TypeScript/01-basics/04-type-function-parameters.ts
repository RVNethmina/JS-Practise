/**
 * 01-basics — Problem 4: Type function parameters
 *
 * Write:
 * 1. `greet` — required `name: string`, required `age: number`
 * 2. `createUser` — required `name`, optional `email`
 * 3. `applyDiscount` — required `price`, `percentage` with a default of 10
 * 4. `sumAll` — a rest parameter of numbers
 * 5. `repeat(times, action)` — action receives the current index, returns void
 *
 * Must compile:
 * - calling each correctly, including omitting optional/defaulted params
 *
 * Must be rejected:
 * - calling greet with one argument
 * - calling greet with three arguments
 * - calling greet with arguments in the wrong order
 * - passing repeat a callback that expects a string instead of a number
 *
 * Answer without looking once done: why must optional parameters come after
 * required ones?
 *
 * Theory: TS-Vault/01-basics/Type Annotations and Inference.md
 */

// your code here

export {};
