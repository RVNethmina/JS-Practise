/**
 * 01-basics — Problem 3: Type tuples
 *
 * Declare:
 * - a coordinate [number, number]
 * - an RGB triple [number, number, number]
 * - a named tuple [status: number, message: string]
 * - a tuple with an optional third element
 * - a tuple with a rest element ([string, ...number[]])
 * - a readonly tuple
 * - a useState-shaped value: [current: number, setter: (next: number) => void]
 *
 * Must compile:
 * - destructuring each tuple
 * - reading each position directly
 *
 * Must be rejected:
 * - wrong element order
 * - too many elements
 * - too few elements
 * - writing to a readonly tuple element
 *
 * Answer without looking once done: why does `const point = [1, 2]` NOT give
 * you a tuple, and what does `as const` change?
 *
 * Theory: TS-Vault/01-basics/Arrays and Tuples.md
 */

// your code here

export {};
