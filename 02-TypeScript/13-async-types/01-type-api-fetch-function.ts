/**
 * 13-async-types — Problem 1: Type API fetch function
 *
 * Define a `User` type. Write `async function fetchUser(id): Promise<User>`
 * and await it inside a `run()` function.
 *
 * Must compile:
 * - const user: User = await fetchUser(1)
 *
 * Must be rejected:
 * - an async function annotated as returning plain `number`
 *
 * Answer without looking once done: why MUST an async function's return type
 * be a Promise? What does TypeScript do if you write a non-Promise type?
 *
 * Theory: TS-Vault/13-async-types/Promise Types and async Returns.md
 */

// your code here

export {};
