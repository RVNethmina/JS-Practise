/**
 * 06-type-aliases — Problem 6: Nested tree type
 *
 * Define a recursive `TreeNode` type with `value: number` and
 * `children: TreeNode[]`. Build a small tree and write
 * `sumTree(node): number` that recursively totals every value.
 *
 * Must compile:
 * - sumTree(tree) on a two-level tree
 *
 * Must be rejected:
 * - a tree whose child has a string `value`
 *
 * Answer without looking once done: how can a type alias reference itself
 * without infinite recursion? What makes this terminate?
 *
 * Theory: TS-Vault/06-type-aliases/Recursive Types.md
 */

// your code here

export {};
