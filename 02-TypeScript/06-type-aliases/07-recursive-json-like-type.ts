/**
 * 06-type-aliases — Problem 7: Recursive JSON-like type
 *
 * Define `JsonValue` covering everything valid JSON can hold: string,
 * number, boolean, null, an array of JsonValue, or an object whose values
 * are JsonValue. Create a nested example.
 *
 * Must compile:
 * - a nested object with a string, boolean, number array, and nested object
 *
 * Must be rejected:
 * - an object containing a function value
 *
 * Answer without looking once done: why does a function value fail this
 * type, and what does that tell you about what JSON.stringify would do
 * with it?
 *
 * Theory: TS-Vault/06-type-aliases/Recursive Types.md
 */

// your code here

export {};
