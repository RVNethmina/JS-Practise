/**
 * 06-type-aliases — Problem 1: User role type
 *
 * Define `UserRoleType` as a literal union and `grantAccess(role): string`.
 *
 * Must compile:
 * - grantAccess("admin")
 *
 * Must be rejected:
 * - grantAccess("superuser")
 *
 * Answer without looking once done: a type alias creates a new *name*, not a
 * new type. What does that mean for two aliases with identical shapes?
 *
 * Theory: TS-Vault/06-type-aliases/Type Aliases Fundamentals.md
 */

// your code here

export {};
