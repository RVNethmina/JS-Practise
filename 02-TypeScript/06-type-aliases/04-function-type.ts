/**
 * 06-type-aliases — Problem 4: Function type
 *
 * Define `Predicate<T> = (value: T) => boolean` and
 * `Mapper<T, R> = (value: T) => R`. Create `isEven` and `double` using them.
 *
 * Must compile:
 * - isEven(4), double(4)
 *
 * Must be rejected:
 * - a Predicate<number> implemented with a string parameter
 *
 * Answer without looking once done: why is a generic function *type alias*
 * different from a generic function *declaration*? Where does the type
 * parameter get fixed in each case?
 *
 * Theory: TS-Vault/06-type-aliases/Type Aliases Fundamentals.md
 */

// your code here

export {};
