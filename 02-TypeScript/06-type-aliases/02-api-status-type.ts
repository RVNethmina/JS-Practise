/**
 * 06-type-aliases — Problem 2: API status type
 *
 * Define `ApiStatusType` as "idle" | "pending" | "fulfilled" | "rejected".
 * Declare a `let status` of that type and reassign it.
 *
 * Must compile:
 * - status = "pending"
 *
 * Must be rejected:
 * - status = "done"
 *
 * Answer without looking once done: why does declaring this with `let`
 * rather than `const` matter for what you can assign later?
 *
 * Theory: TS-Vault/06-type-aliases/Type Aliases Fundamentals.md
 */

// your code here

export {};
