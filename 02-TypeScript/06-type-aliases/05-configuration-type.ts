/**
 * 06-type-aliases — Problem 5: Configuration type
 *
 * Define `ConfigurationType` with host, port, useTls. Create one valid
 * config.
 *
 * Must compile:
 * - a complete config
 *
 * Must be rejected:
 * - a config whose `port` is a string
 *
 * Answer without looking once done: when would you reach for a type alias
 * here instead of an interface? Is there a functional difference for this
 * particular shape?
 *
 * Theory: TS-Vault/06-type-aliases/Type Aliases Fundamentals.md
 */

// your code here

export {};
