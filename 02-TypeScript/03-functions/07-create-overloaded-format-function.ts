/**
 * 03-functions — Problem 7: Create overloaded format function
 *
 * Write overload signatures for `format`: one accepting `number`, one
 * `boolean`, one `Date` — each returning `string` — plus a single wider
 * implementation signature that handles all three.
 *
 * Must compile:
 * - format(3.14159), format(true), format(new Date())
 *
 * Must be rejected:
 * - format("not allowed")
 *
 * Answer without looking once done: why do you need a separate, wider
 * implementation signature underneath the overloads, and is that
 * implementation signature visible to callers?
 *
 * Theory: TS-Vault/03-functions/Function Overloads.md
 */

// your code here

export {};
