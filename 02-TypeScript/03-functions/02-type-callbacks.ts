/**
 * 03-functions — Problem 2: Type callbacks
 *
 * Define `SuccessCallback = (data: string) => void` and
 * `ErrorCallback = (error: Error) => void`. Write
 * `performAction(onSuccess, onError): void`.
 *
 * Must compile:
 * - performAction with correctly typed callbacks
 *
 * Must be rejected:
 * - performAction with onSuccess typed to take a number instead of a string
 *
 * Answer without looking once done: what's the practical difference between
 * two separate callback parameters (success/error) versus one callback that
 * receives a discriminated union result?
 *
 * Theory: TS-Vault/03-functions/Function Type Aliases and Callbacks.md
 */

// your code here

export {};
