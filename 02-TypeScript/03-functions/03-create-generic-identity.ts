/**
 * 03-functions — Problem 3: Create generic identity
 *
 * Write `identity<T>(value: T): T`.
 *
 * Must compile:
 * - identity(42), identity("hi"), identity<boolean>(true)
 *
 * Must be rejected:
 * - assigning the result of identity(42) into a variable typed `string`
 *
 * Answer without looking once done: what does the generic signature let you
 * preserve that `identity(value: any): any` would lose?
 *
 * Theory: TS-Vault/03-functions/Generic Functions.md
 */

// your code here

export {};
