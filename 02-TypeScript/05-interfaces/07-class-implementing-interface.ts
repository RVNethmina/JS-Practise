/**
 * 05-interfaces — Problem 7: Class implementing interface
 *
 * Declare `interface Shape` with `area(): number` and
 * `perimeter(): number`. Implement it in a `Rectangle` class using
 * constructor parameter properties. Then write a broken class missing
 * `perimeter`.
 *
 * Must compile:
 * - new Rectangle(4, 5).area()
 *
 * Must be rejected:
 * - a class declaring `implements Shape` with only `area`
 *
 * Answer without looking once done: what does `constructor(private width:
 * number)` do that a plain `constructor(width: number)` doesn't?
 *
 * Theory: TS-Vault/05-interfaces/Implementing Interfaces in Classes.md
 */

// your code here

export {};
