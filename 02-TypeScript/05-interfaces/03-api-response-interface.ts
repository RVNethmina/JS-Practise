/**
 * 05-interfaces — Problem 3: API response interface
 *
 * Declare a generic `interface ApiResponseInterface<T>` with
 * `status: number`, `data: T`, `error: string | null`. Create one for a
 * `{ id: number }` payload.
 *
 * Must compile:
 * - a complete response
 *
 * Must be rejected:
 * - a response missing `error`
 *
 * Answer without looking once done: why make the response generic over its
 * payload rather than typing `data` as `unknown`?
 *
 * Theory: TS-Vault/05-interfaces/Generic Interfaces.md
 */

// your code here

export {};
