/**
 * 05-interfaces — Problem 2: Admin interface
 *
 * Declare `UserInterface` (id, name) and `AdminInterface extends
 * UserInterface` adding `permissions: string[]`.
 *
 * Must compile:
 * - a complete admin
 *
 * Must be rejected:
 * - an admin missing `permissions`
 *
 * Answer without looking once done: how does `interface X extends Y` differ
 * from `type X = Y & { ... }` in practice?
 *
 * Theory: TS-Vault/05-interfaces/Interface Extension and Merging.md
 */

// your code here

export {};
