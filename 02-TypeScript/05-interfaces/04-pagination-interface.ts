/**
 * 05-interfaces — Problem 4: Pagination interface
 *
 * Declare `interface PaginationInterface<T>` with items, page, pageSize,
 * total. Write `hasNextPage<T>(pagination): boolean`.
 *
 * Must compile:
 * - hasNextPage on a valid page object
 *
 * Must be rejected:
 * - a page object whose `page` is a string
 *
 * Answer without looking once done: why does `hasNextPage` need its own `<T>`
 * parameter even though it never touches an individual item?
 *
 * Theory: TS-Vault/05-interfaces/Generic Interfaces.md
 */

// your code here

export {};
