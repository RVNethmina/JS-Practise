/**
 * 05-interfaces — Problem 1: User interface
 *
 * Declare `interface UserInterface` with id, name, email. Create one valid
 * user.
 *
 * Must compile:
 * - a complete user
 *
 * Must be rejected:
 * - a user missing `email`
 *
 * Answer without looking once done: what can an `interface` do that a `type`
 * alias cannot, and vice versa? This is one of the most asked TS interview
 * questions.
 *
 * Theory: TS-Vault/05-interfaces/Interface vs Type Alias.md
 */

// your code here

export {};
