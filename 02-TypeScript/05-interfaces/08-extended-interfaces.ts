/**
 * 05-interfaces — Problem 8: Extended interfaces
 *
 * Build a three-level chain: `Animal` (name, makeSound()), `Pet extends
 * Animal` (owner), `ServiceAnimal extends Pet` (certification). Create one
 * valid ServiceAnimal.
 *
 * Must compile:
 * - a complete ServiceAnimal with all inherited members
 *
 * Must be rejected:
 * - a ServiceAnimal missing `certification`
 *
 * Answer without looking once done: can an interface extend more than one
 * interface at a time? What happens if two parents declare the same property
 * with different types?
 *
 * Theory: TS-Vault/05-interfaces/Interface Extension and Merging.md
 */

// your code here

export {};
