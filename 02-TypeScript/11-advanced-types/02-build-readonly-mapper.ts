/**
 * 11-advanced-types — Problem 2: Build readonly mapper
 *
 * Implement `MyReadonly<T>` from scratch using a mapped type with the
 * `readonly` modifier. Apply it to a Config type.
 *
 * Must compile:
 * - creating a valid readonly config
 *
 * Must be rejected:
 * - mutating one of its properties
 *
 * Answer without looking once done: mapped types can ADD modifiers with
 * `readonly` and `?`. What syntax REMOVES them?
 *
 * Theory: TS-Vault/11-advanced-types/Mapped Types.md
 */

// your code here

export {};
