/**
 * 11-advanced-types — Problem 3: Extract function return types
 *
 * Implement `MyReturnType<F>` from scratch using a conditional type with
 * `infer` — recreating the built-in ReturnType. Apply it to a function.
 *
 * Must compile:
 * - the extracted type matching the function's real return shape
 *
 * Must be rejected:
 * - an object of the wrong shape assigned to it
 *
 * Answer without looking once done: what does `infer R` actually do? Where
 * does R come from, and when is it bound?
 *
 * Theory: TS-Vault/11-advanced-types/Conditional Types and infer.md
 */

// your code here

export {};
