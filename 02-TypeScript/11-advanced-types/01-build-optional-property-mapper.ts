/**
 * 11-advanced-types — Problem 1: Build optional property mapper
 *
 * Implement `MyPartial<T>` from scratch using a mapped type with the `?`
 * modifier — recreating the built-in Partial. Apply it to a User type.
 *
 * Must compile:
 * - a partial object with only some fields
 *
 * Must be rejected:
 * - a partial object with a field of the wrong type
 *
 * Answer without looking once done: read `[K in keyof T]?: T[K]` out loud in
 * plain English. What is K, and what is it iterating over?
 *
 * Theory: TS-Vault/11-advanced-types/Mapped Types.md
 */

// your code here

export {};
