/**
 * 04-objects — Problem 2: Type product object
 *
 * Define `Product` with id, name, price, and an OPTIONAL `tags: string[]`.
 * Create one product without tags and one with tags.
 *
 * Must compile:
 * - both products
 *
 * Must be rejected:
 * - a product whose `price` is a string
 *
 * Answer without looking once done: is `tags?: string[]` exactly the same as
 * `tags: string[] | undefined`? What can you do with one that you can't with
 * the other?
 *
 * Theory: TS-Vault/04-objects/Optional and Readonly Properties.md
 */

// your code here

export {};
