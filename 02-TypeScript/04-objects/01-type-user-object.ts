/**
 * 04-objects — Problem 1: Type user object
 *
 * Define an `Address` type (street, city, zip) and a `UserObject` type
 * (id, name, email, address) that nests it. Create one valid user.
 *
 * Must compile:
 * - a complete user object
 *
 * Must be rejected:
 * - a user object missing the `address` field
 *
 * Answer without looking once done: does TypeScript care what your type is
 * *named* when checking assignability, or only what shape it has?
 *
 * Theory: TS-Vault/04-objects/Object Type Syntax and Nesting.md
 */

// your code here

export {};
