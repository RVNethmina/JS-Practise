/**
 * 04-objects — Problem 3: Type nested API response
 *
 * Define `ApiResponse` with `status: number` and a deeply nested
 * `data.user.profile` containing `displayName: string` and
 * `avatarUrl: string | null`. Create one valid response and read the
 * displayName out of it.
 *
 * Must compile:
 * - the full response object, and reading response.data.user.profile.displayName
 *
 * Must be rejected:
 * - a response where `user` is missing its `profile`
 *
 * Answer without looking once done: at what depth does it become worth
 * splitting a nested inline type into separate named types?
 *
 * Theory: TS-Vault/04-objects/Object Type Syntax and Nesting.md
 */

// your code here

export {};
