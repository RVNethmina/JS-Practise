/**
 * 04-objects — Problem 6: Type dynamic key object
 *
 * Define `ScoreBoard` as `Record<string, number>`. Create a scores object,
 * add a key, and write `totalScore(board): number` using Object.values.
 *
 * Must compile:
 * - totalScore(scores)
 *
 * Must be rejected:
 * - assigning a string as a score value
 *
 * Answer without looking once done: `Record<string, number>` and
 * `{ [key: string]: number }` — are these the same thing? When would you
 * reach for Record instead?
 *
 * Theory: TS-Vault/04-objects/Index Signatures.md
 */

// your code here

export {};
