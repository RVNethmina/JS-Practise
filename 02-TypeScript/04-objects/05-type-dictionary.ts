/**
 * 04-objects — Problem 5: Type dictionary
 *
 * Define `Dictionary` using an index signature so any string key maps to a
 * string value. Create a translations object, add a key dynamically, and
 * read one out.
 *
 * Must compile:
 * - translations.thanks = "sthuthi"
 * - const greeting: string = translations["hello"]
 *
 * Must be rejected:
 * - assigning a number as a value
 *
 * Answer without looking once done: with an index signature, what does
 * TypeScript assume about a key you haven't explicitly set — does it warn
 * you the value might be undefined?
 *
 * Theory: TS-Vault/04-objects/Index Signatures.md
 */

// your code here

export {};
