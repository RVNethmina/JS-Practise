/**
 * 10-utility-types — Problem 5: Create role map
 *
 * Define `Role = "admin" | "editor" | "viewer"` and
 * `RolePermissions = Record<Role, string[]>`. Create a complete permissions
 * map.
 *
 * Must compile:
 * - a map with all three roles
 *
 * Must be rejected:
 * - a map missing the "viewer" role
 *
 * Answer without looking once done: why does `Record<Role, string[]>`
 * require ALL three keys, while `Record<string, string[]>` requires none?
 *
 * Theory: TS-Vault/10-utility-types/Record and Key Mapping.md
 */

// your code here

export {};
