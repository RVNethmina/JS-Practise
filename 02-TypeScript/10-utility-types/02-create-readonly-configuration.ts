/**
 * 10-utility-types — Problem 2: Create readonly configuration
 *
 * Define `MutableConfig` (host, port) and derive
 * `ReadonlyConfig = Readonly<MutableConfig>`.
 *
 * Must compile:
 * - creating a valid config
 *
 * Must be rejected:
 * - config.port = 9090
 *
 * Answer without looking once done: is `Readonly<T>` deep or shallow? What
 * happens to a nested object property?
 *
 * Theory: TS-Vault/10-utility-types/Partial, Required, Readonly.md
 */

// your code here

export {};
