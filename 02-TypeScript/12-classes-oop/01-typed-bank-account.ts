/**
 * 12-classes-oop — Problem 1: Typed bank account
 *
 * Write a `BankAccount` class with a `private balance`, a
 * `private readonly accountHolder` constructor parameter property, and
 * deposit / withdraw / getBalance methods that validate their input.
 *
 * Must compile:
 * - account.deposit(50), account.getBalance()
 *
 * Must be rejected:
 * - reading account.balance from outside the class
 *
 * Answer without looking once done: is TypeScript's `private` enforced at
 * runtime? How does it differ from JavaScript's `#private` fields?
 *
 * Theory: TS-Vault/12-classes-oop/Access Modifiers.md
 */

// your code here

export {};
