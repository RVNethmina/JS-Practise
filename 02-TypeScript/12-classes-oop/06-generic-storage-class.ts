/**
 * 12-classes-oop — Problem 6: Generic storage class
 *
 * Write `class Storage<T>` with a private items array and add / get / size
 * methods. Instantiate it as `Storage<number>`.
 *
 * Must compile:
 * - numberStorage.add(1)
 *
 * Must be rejected:
 * - numberStorage.add("3")
 *
 * Answer without looking once done: `get(index)` returns `T | undefined`.
 * Why not just `T`, given the array only ever holds T?
 *
 * Theory: TS-Vault/12-classes-oop/Generics with Classes.md
 */

// your code here

export {};
