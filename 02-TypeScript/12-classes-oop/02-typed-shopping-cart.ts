/**
 * 12-classes-oop — Problem 2: Typed shopping cart
 *
 * Define `CartItem = { productId, price, quantity }`. Write a
 * `ShoppingCart` class with a private items array and addItem / removeItem /
 * getTotal methods.
 *
 * Must compile:
 * - cart.addItem({ productId: 1, price: 10, quantity: 2 }) and cart.getTotal()
 *
 * Must be rejected:
 * - cart.addItem({ productId: 1, price: 10 })  (missing quantity)
 *
 * Answer without looking once done: `getTotal` uses reduce with an initial
 * value of 0. What would the inferred return type be if you omitted that
 * initial value?
 *
 * Theory: TS-Vault/12-classes-oop/Access Modifiers.md
 */

// your code here

export {};
