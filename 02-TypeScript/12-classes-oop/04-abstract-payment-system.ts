/**
 * 12-classes-oop — Problem 4: Abstract payment system
 *
 * Write `abstract class PaymentMethod` with an abstract `pay(amount):
 * string` and a concrete `describe(): string`. Implement
 * CreditCardPayment and PayPalPayment.
 *
 * Must compile:
 * - calling pay on both concrete classes
 *
 * Must be rejected:
 * - `new PaymentMethod()`
 *
 * Answer without looking once done: what's the difference between an
 * abstract class and an interface here? When does the abstract class earn
 * its keep?
 *
 * Theory: TS-Vault/12-classes-oop/Abstract Classes and Inheritance.md
 */

// your code here

export {};
