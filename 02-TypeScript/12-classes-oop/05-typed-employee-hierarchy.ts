/**
 * 12-classes-oop — Problem 5: Typed employee hierarchy
 *
 * Write `class Employee` with `protected name` and `protected salary`, plus
 * `getAnnualSalary()`. Write `class Manager extends Employee` that adds a
 * private teamSize and OVERRIDES getAnnualSalary using `super`.
 *
 * Must compile:
 * - manager.getAnnualSalary()
 *
 * Must be rejected:
 * - reading manager.salary from outside the class
 *
 * Answer without looking once done: what's the difference between `private`
 * and `protected`, and why is `salary` protected rather than private here?
 *
 * Theory: TS-Vault/12-classes-oop/Access Modifiers.md
 *         TS-Vault/12-classes-oop/Abstract Classes and Inheritance.md
 */

// your code here

export {};
