/**
 * 02-types — Problem 4: Combine object types
 *
 * Define `WithId = { id: number }` and `WithTimestamps = { createdAt: string;
 * updatedAt: string }`. Combine them with an intersection into `Entity`.
 *
 * Must compile:
 * - an Entity object with all three fields
 *
 * Must be rejected:
 * - an Entity object missing one of the fields
 *
 * Answer without looking once done: how does intersection (`&`) differ from
 * union (`|`) in terms of which values satisfy the resulting type?
 *
 * Theory: TS-Vault/02-types/Unions and Intersections.md
 */

// your code here

export {};
