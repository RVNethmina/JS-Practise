/**
 * 02-types — Problem 2: Create status union
 *
 * Define `RequestStatus` as "idle" | "loading" | "success" | "error". Write
 * `describeStatus(status): string` using a switch that returns a human
 * readable description for each case.
 *
 * Must compile:
 * - describeStatus("loading")
 *
 * Must be rejected:
 * - describeStatus("done")
 *
 * Answer without looking once done: if you add a fifth status to the union
 * later and forget to handle it in the switch, does TypeScript catch that by
 * default? What would you need to add to force it to?
 *
 * Theory: TS-Vault/02-types/Unions and Intersections.md
 */

// your code here

export {};
