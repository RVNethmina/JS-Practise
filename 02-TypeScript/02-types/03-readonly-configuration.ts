/**
 * 02-types — Problem 3: Create readonly configuration
 *
 * Define `ReadonlyConfig` with `readonly apiKey: string` and
 * `readonly maxRetries: number`. Create one valid config object.
 *
 * Must compile:
 * - reading config.apiKey
 *
 * Must be rejected:
 * - config.apiKey = "changed"
 *
 * Answer without looking once done: does `readonly` stop the object's
 * contents from ever changing, or only stop reassignment of that property?
 *
 * Theory: TS-Vault/02-types/Literal Types and Readonly Properties.md
 */

// your code here

export {};
