/**
 * 08-generics — Problem 3: Generic first element
 *
 * Write `first<T>(items: T[]): T | undefined`.
 *
 * Must compile:
 * - const firstNum: number | undefined = first([1, 2, 3])
 *
 * Must be rejected:
 * - const bad: number = first([1, 2, 3])
 *
 * Answer without looking once done: why must the return type include
 * `undefined`? What does `noUncheckedIndexedAccess` have to do with this?
 *
 * Theory: TS-Vault/08-generics/Generic Fundamentals.md
 */

// your code here

export {};
