/**
 * 08-generics — Problem 1: Generic identity function
 *
 * Write `identity<T>(value: T): T`. Call it with a number and a string.
 *
 * Must compile:
 * - const a: number = identity(5)
 * - const b: string = identity("hi")
 *
 * Must be rejected:
 * - const c: string = identity(5)
 *
 * Answer without looking once done: when you call `identity(5)` without
 * angle brackets, how does TypeScript decide what `T` is?
 *
 * Theory: TS-Vault/08-generics/Generic Fundamentals.md
 */

// your code here

export {};
