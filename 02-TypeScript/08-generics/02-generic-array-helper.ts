/**
 * 08-generics — Problem 2: Generic array helper
 *
 * Write `toArray<T>(value: T): T[]` that wraps a single value in an array.
 *
 * Must compile:
 * - const nums: number[] = toArray(5)
 *
 * Must be rejected:
 * - const bad: string[] = toArray(5)
 *
 * Answer without looking once done: what would break if you typed this as
 * `toArray(value: unknown): unknown[]` instead?
 *
 * Theory: TS-Vault/08-generics/Generic Fundamentals.md
 */

// your code here

export {};
